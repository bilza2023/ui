


{
    id: xxx
    name : xxxx
    data : {},
    theme : {},
    mapTheme : // default implementation we add to it
    getItems: // same
}

now this is an object that can be consumed by Deck builder and used . it does not call any one it is a utility object ? 
no new to add items we dont expect
no auto triggers 