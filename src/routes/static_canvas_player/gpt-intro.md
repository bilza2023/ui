The sole purpose of this slide generator is to output slide.js file with correct formatting of "slide" object as explained in the instructions

In simple words you have to

1: add /edit fields in background (as in slide.background)  
2: add items (item objects) in the slide.items

Main Task:

==> The main task is to add fields (items and backgound fields) keeping in view what there values can be.

What Never to do:

==> Do not change or remove or add fields (items and backgound fields)
